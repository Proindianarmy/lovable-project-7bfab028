# Use relative /api — Vite dev proxy handles routing to backend.
# For cross-domain production, set: VITE_API_URL=https://your-backend.com/api
VITE_API_URL=/api

# VITE_SIGHTENGINE_USER=your_user
# VITE_SIGHTENGINE_SECRET=your_secret
